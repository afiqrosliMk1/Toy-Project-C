#include <stdio.h>

int main( void )
{
    //printf( "\'" );
    //printf( "\"" );
    //printf( "\?" );
    //printf( "\\" );
    //printf( "\a" );

    //printf( "t\b" );
    //printf( "Move cursor back one\b position on current line\n" );
    //printf( "Move cursor back one\t position on current line\n" );
    //printf( "Move cursor back one\f position on current line\n" );
    //printf( "Move cursor back one\r position on current line\n" );
    printf( "Move cursor back one\v position on current line\n" );
    printf( "Move cursor back one position on current line\n" );


    return 0;
}
